---
summary: "Deploy SunClaw on Railway with one-click template"
read_when:
  - Deploying SunClaw to Railway
  - You want a one-click cloud deploy with browser-based Control UI
title: "Railway"
---

# Railway

Deploy SunClaw on Railway with a one-click template and access it through the web Control UI.
This is the easiest "no terminal on the server" path: Railway runs the Gateway for you.

## Quick checklist (new users)

1. Click **Deploy on Railway** (below).
2. Add a **Volume** mounted at `/data`.
3. Set the required **Variables** (at least `SUNCLAW_GATEWAY_PORT` and `SUNCLAW_GATEWAY_TOKEN`).
4. Enable **HTTP Proxy** on port `8080`.
5. Open `https://<your-railway-domain>/sunclaw` and connect using the configured shared secret. This template uses `SUNCLAW_GATEWAY_TOKEN` by default; if you replace it with password auth, use that password instead.

## One-click deploy

<a href="https://railway.com/deploy/clawdbot-railway-template" target="_blank" rel="noreferrer">
  Deploy on Railway
</a>

After deploy, find your public URL in **Railway → your service → Settings → Domains**.

Railway will either:

- give you a generated domain (often `https://<something>.up.railway.app`), or
- use your custom domain if you attached one.

Then open:

- `https://<your-railway-domain>/sunclaw` — Control UI

## What you get

- Hosted SunClaw Gateway + Control UI
- Persistent storage via Railway Volume (`/data`) so `sunclaw.json`,
  per-agent `auth-profiles.json`, channel/provider state, sessions, and
  workspace survive redeploys

## Required Railway settings

### Public Networking

Enable **HTTP Proxy** for the service.

- Port: `8080`

### Volume (required)

Attach a volume mounted at:

- `/data`

### Variables

Set these variables on the service:

- `SUNCLAW_GATEWAY_PORT=8080` (required — must match the port in Public Networking)
- `SUNCLAW_GATEWAY_TOKEN` (required; treat as an admin secret)
- `SUNCLAW_STATE_DIR=/data/.sunclaw` (recommended)
- `SUNCLAW_WORKSPACE_DIR=/data/workspace` (recommended)

## Connect a channel

Use the Control UI at `/sunclaw` or run `sunclaw onboard` via Railway's shell for channel setup instructions:

- [Telegram](/channels/telegram) (fastest — just a bot token)
- [Discord](/channels/discord)
- [All channels](/channels)

## Backups & migration

Export your state, config, auth profiles, and workspace:

```bash
sunclaw backup create
```

This creates a portable backup archive with SunClaw state plus any configured
workspace. See [Backup](/cli/backup) for details.

## Next steps

- Set up messaging channels: [Channels](/channels)
- Configure the Gateway: [Gateway configuration](/gateway/configuration)
- Keep SunClaw up to date: [Updating](/install/updating)
