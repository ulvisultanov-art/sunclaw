interface:
  display_name: "SunClaw Debugging"
  short_description: "Debug model, tool, stream, and live behavior"
  default_prompt: "Use $sunclaw-debugging to identify the right SunClaw debug boundary, turn on targeted logs, and choose the narrowest local or Crabbox proof."
