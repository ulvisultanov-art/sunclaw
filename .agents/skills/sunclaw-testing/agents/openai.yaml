interface:
  display_name: "SunClaw Testing"
  short_description: "Choose cheap, targeted SunClaw validation"
  default_prompt: "Use $sunclaw-testing to choose the cheapest safe test or CI verification path, inspect failures, and rerun only the relevant SunClaw lane."
