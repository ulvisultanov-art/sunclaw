apiVersion: v1
kind: Service
metadata:
  name: sunclaw
  labels:
    app: sunclaw
spec:
  type: ClusterIP
  selector:
    app: sunclaw
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
