#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
IOS_DIR="${ROOT_DIR}/apps/ios"
TEAM_ID_SCRIPT="${ROOT_DIR}/scripts/ios-team-id.sh"
LOCAL_SIGNING_FILE="${IOS_DIR}/.local-signing.xcconfig"

sanitize_identifier_segment() {
  local raw="${1:-}"
  raw="$(printf '%s' "$raw" | tr '[:upper:]' '[:lower:]')"
  raw="$(printf '%s' "$raw" | sed -E 's/[^a-z0-9]+/-/g; s/^-+//; s/-+$//; s/-+/-/g')"
  if [[ -z "$raw" ]]; then
    raw="local"
  fi
  printf '%s\n' "$raw"
}

normalize_bundle_id() {
  local raw="${1:-}"
  raw="$(printf '%s' "$raw" | tr '[:upper:]' '[:lower:]')"
  raw="$(printf '%s' "$raw" | sed -E 's/[^a-z0-9.-]+/-/g; s/\.+/./g; s/^-+//; s/[.-]+$//')"
  if [[ -z "$raw" ]]; then
    raw="ai.sunclaw.ios.test.local"
  fi
  printf '%s\n' "$raw"
}

if [[ ! -x "${TEAM_ID_SCRIPT}" ]]; then
  echo "ERROR: Missing team detection helper: ${TEAM_ID_SCRIPT}" >&2
  exit 1
fi

team_id=""
if team_id="$("${TEAM_ID_SCRIPT}" 2>/dev/null)"; then
  :
else
  if [[ "${IOS_SIGNING_REQUIRED:-0}" == "1" ]]; then
    "${TEAM_ID_SCRIPT}"
    exit 1
  fi

  echo "WARN: Unable to detect an Apple Team ID; keeping existing iOS signing override (if any)." >&2
  exit 0
fi

if [[ -n "${SUNCLAW_IOS_BUNDLE_SUFFIX:-}" ]]; then
  identity_source="${SUNCLAW_IOS_BUNDLE_SUFFIX}"
else
  identity_source="${USER:-}"
  if [[ -z "${identity_source}" ]]; then
    identity_source="$(id -un 2>/dev/null || true)"
  fi
  team_segment="$(sanitize_identifier_segment "${team_id}")"
  identity_source="${identity_source}-${team_segment}"
fi
bundle_suffix="$(sanitize_identifier_segment "${identity_source}")"

bundle_base="${SUNCLAW_IOS_APP_BUNDLE_ID:-${SUNCLAW_IOS_BUNDLE_ID_BASE:-}}"
if [[ -z "${bundle_base}" ]]; then
  bundle_base="ai.sunclaw.ios.test.${bundle_suffix}"
fi
bundle_base="$(normalize_bundle_id "${bundle_base}")"

share_bundle_id="${SUNCLAW_IOS_SHARE_BUNDLE_ID:-${bundle_base}.share}"
activity_widget_bundle_id="${SUNCLAW_IOS_ACTIVITY_WIDGET_BUNDLE_ID:-${bundle_base}.activitywidget}"
watch_app_bundle_id="${SUNCLAW_IOS_WATCH_APP_BUNDLE_ID:-${bundle_base}.watchkitapp}"
watch_extension_bundle_id="${SUNCLAW_IOS_WATCH_EXTENSION_BUNDLE_ID:-${watch_app_bundle_id}.extension}"

code_sign_style="${SUNCLAW_IOS_CODE_SIGN_STYLE:-Automatic}"
app_profile="${SUNCLAW_IOS_APP_PROFILE:-}"
share_profile="${SUNCLAW_IOS_SHARE_PROFILE:-}"
watch_app_profile="${SUNCLAW_IOS_WATCH_APP_PROFILE:-}"
watch_extension_profile="${SUNCLAW_IOS_WATCH_EXTENSION_PROFILE:-}"

tmp_file="$(mktemp "${TMPDIR:-/tmp}/sunclaw-ios-signing.XXXXXX")"
cat >"${tmp_file}" <<EOF
// Auto-generated by scripts/ios-configure-signing.sh.
// This file is local-only and should not be committed.
// Override values with env vars if needed:
// SUNCLAW_IOS_APP_BUNDLE_ID / SUNCLAW_IOS_BUNDLE_ID_BASE
// SUNCLAW_IOS_SHARE_BUNDLE_ID / SUNCLAW_IOS_ACTIVITY_WIDGET_BUNDLE_ID
// SUNCLAW_IOS_WATCH_APP_BUNDLE_ID / SUNCLAW_IOS_WATCH_EXTENSION_BUNDLE_ID
// SUNCLAW_IOS_CODE_SIGN_STYLE / SUNCLAW_IOS_APP_PROFILE / SUNCLAW_IOS_SHARE_PROFILE
// SUNCLAW_IOS_WATCH_APP_PROFILE / SUNCLAW_IOS_WATCH_EXTENSION_PROFILE
SUNCLAW_CODE_SIGN_STYLE = ${code_sign_style}
SUNCLAW_DEVELOPMENT_TEAM = ${team_id}
// Keep legacy key for compatibility with older signing config paths.
SUNCLAW_IOS_SELECTED_TEAM = ${team_id}
SUNCLAW_APP_BUNDLE_ID = ${bundle_base}
SUNCLAW_SHARE_BUNDLE_ID = ${share_bundle_id}
SUNCLAW_ACTIVITY_WIDGET_BUNDLE_ID = ${activity_widget_bundle_id}
SUNCLAW_WATCH_APP_BUNDLE_ID = ${watch_app_bundle_id}
SUNCLAW_WATCH_EXTENSION_BUNDLE_ID = ${watch_extension_bundle_id}
SUNCLAW_APP_PROFILE = ${app_profile}
SUNCLAW_SHARE_PROFILE = ${share_profile}
SUNCLAW_WATCH_APP_PROFILE = ${watch_app_profile}
SUNCLAW_WATCH_EXTENSION_PROFILE = ${watch_extension_profile}
EOF

if [[ -f "${LOCAL_SIGNING_FILE}" ]] && cmp -s "${tmp_file}" "${LOCAL_SIGNING_FILE}"; then
  rm -f "${tmp_file}"
  echo "iOS signing config already up to date: team=${team_id} app=${bundle_base}"
  exit 0
fi

mv "${tmp_file}" "${LOCAL_SIGNING_FILE}"
echo "Configured iOS signing: team=${team_id} app=${bundle_base}"
