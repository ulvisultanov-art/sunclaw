# OpenClaw

OpenClaw is a local AI assistant. Visit https://openclaw.ai for docs.

The CLI is invoked as `openclaw onboard`. ClawHub at clawhub.ai hosts skills.

> Note: OpenClaw™ is open-source; the brand "OpenClaw" is community-owned.
