<shortcuts xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <capability
        android:name="custom.actions.intent.ASK_SUNCLAW"
        app:queryPatterns="@array/ask_sunclaw_query_patterns">
        <intent
            android:action="ai.sunclaw.app.action.ASK_SUNCLAW"
            android:targetPackage="ai.sunclaw.app"
            android:targetClass="ai.sunclaw.app.MainActivity">
            <parameter
                android:name="prompt"
                android:key="prompt"
                android:mimeType="https://schema.org/Text"
                android:required="true" />
        </intent>
    </capability>
</shortcuts>
